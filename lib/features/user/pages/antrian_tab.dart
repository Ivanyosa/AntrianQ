import 'package:flutter/material.dart';

class AntrianTab extends StatelessWidget {
  const AntrianTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Antrian'),
    );
  }
}